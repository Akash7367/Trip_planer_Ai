# Docker Configuration Directory

This directory stores environment-specific configuration files, custom setup scripts, and utilities for containerization.
The primary environment orchestration is configured via the root [docker-compose.yml](../docker-compose.yml).
